import { useState, useRef, useEffect } from "react";
import { Text, View, TouchableOpacity } from "react-native";
import { createData } from "../utils/db";

export default function CameraResult() {
  return (
    <View>
      <Text> Im a new page </Text>
    </View>
  );
}
